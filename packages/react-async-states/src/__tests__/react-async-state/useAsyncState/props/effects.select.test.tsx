describe('should select from another async state', () => {
  it('should select by source', () => {
    // given

    // when

    // then
  });
  it('should select by key', () => {
    // given

    // when

    // then
  });
  it('should runp producer by function', () => {
    // given

    // when

    // then
  });
});
